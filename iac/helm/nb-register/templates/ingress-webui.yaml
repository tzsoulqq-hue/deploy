{{- if .Values.ingress.webui.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "nb-register.componentFullname" (dict "root" . "component" "webui") }}
  labels:
    {{- include "nb-register.componentLabels" (dict "root" . "component" "webui") | nindent 4 }}
  {{- with .Values.ingress.webui.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.ingress.webui.className }}
  ingressClassName: {{ .Values.ingress.webui.className | quote }}
  {{- end }}
  {{- with .Values.ingress.webui.tls }}
  tls:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  rules:
    {{- range .Values.ingress.webui.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "nb-register.componentFullname" (dict "root" $ "component" "webui") }}
                port:
                  number: 8080
          {{- end }}
    {{- end }}
{{- end }}
