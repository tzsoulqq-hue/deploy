{{- if .Values.workflowRuntimeEngine.enabled }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "byte-v-forge.componentFullname" (dict "root" . "component" "workflow-runtime-engine") }}
  labels:
    {{- include "byte-v-forge.componentLabels" (dict "root" . "component" "workflow-runtime-engine") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: grpc
      port: {{ .Values.workflowRuntimeEngine.service.port }}
      targetPort: grpc
  selector:
    {{- include "byte-v-forge.selectorLabels" (dict "root" . "component" "workflow-runtime-engine") | nindent 4 }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "byte-v-forge.componentFullname" (dict "root" . "component" "workflow-runtime-engine") }}
  labels:
    {{- include "byte-v-forge.componentLabels" (dict "root" . "component" "workflow-runtime-engine") | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "byte-v-forge.selectorLabels" (dict "root" . "component" "workflow-runtime-engine") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "byte-v-forge.selectorLabels" (dict "root" . "component" "workflow-runtime-engine") | nindent 8 }}
    spec:
      serviceAccountName: {{ include "byte-v-forge.serviceAccountName" . }}
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: workflow-runtime-engine
          image: {{ include "byte-v-forge.image" (dict "root" . "image" .Values.workflowRuntimeEngine.image) | quote }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - name: grpc
              containerPort: 7233
          env:
            - name: DB
              value: postgres12
            - name: DB_PORT
              value: {{ include "byte-v-forge.postgresPort" . | quote }}
            - name: POSTGRES_USER
              value: {{ .Values.postgres.auth.user | quote }}
            - name: POSTGRES_PWD
              valueFrom:
                secretKeyRef:
                  name: {{ include "byte-v-forge.appSecretName" . }}
                  key: POSTGRES_PASSWORD
            - name: POSTGRES_SEEDS
              value: {{ include "byte-v-forge.postgresHost" . | quote }}
            - name: DBNAME
              value: {{ .Values.workflowRuntimeEngine.database | quote }}
            - name: VISIBILITY_DBNAME
              value: {{ .Values.workflowRuntimeEngine.visibilityDatabase | quote }}
          readinessProbe:
            tcpSocket:
              port: grpc
          livenessProbe:
            tcpSocket:
              port: grpc
          resources:
            {{- toYaml .Values.workflowRuntimeEngine.resources | nindent 12 }}
{{- end }}
