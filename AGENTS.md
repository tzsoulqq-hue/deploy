# AGENTS.md

本仓是 `base-images`，只维护共享容器基础镜像定义。

- 禁止提交业务代码、业务配置、真实 secret、真实账号或部署环境私有值。
- Dockerfile 只能表达通用构建/运行能力，不嵌入某个业务服务的启动逻辑。
- 镜像版本默认使用最新稳定版本；生产长期运行组件优先 LTS 或维护期内稳定主版本。
- 新增镜像必须补 README 说明、构建目标和 CI 验证。
- Mac 本机不作为业务镜像构建环境；需要真实构建/推送时使用远程构建环境或 CI。
